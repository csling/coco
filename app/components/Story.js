var React = require('react');
var PropTypes = React.PropTypes;

var Story = React.createClass({
  render: function () {
    return (
      <div> Story Page! </div>
    )
  }
});

module.exports = Story;
